{
  "name": "fps-client",
  "version": "1.0.0",
  "scripts": {
    "build": "node copy-shared.js && vite build",
    "dev": "node copy-shared.js && vite",
    "deploy": "node copy-shared.js && npm run build && node deploy.js"
  },
  "dependencies": {
    "socket.io-client": "^4.7.2",
    "three": "^0.160.1",
    "three-mesh-bvh": "^0.9.1"
  },
  "devDependencies": {
    "@gltf-transform/cli": "^4.2.0",
    "dotenv": "^17.0.0",
    "ftp-deploy": "^2.4.7",
    "gltf-pipeline": "^4.1.0",
    "sharp": "^0.34.2",
    "vite": "^5.0.0"
  }
}
